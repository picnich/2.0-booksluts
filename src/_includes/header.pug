- var navTitle = siteSettings.name || 'My website'

header.site-header
  .site-identity
    h1
      a(href="/")= navTitle  
  nav.site-navigation
    ul.nav
      for navLink in siteSettings.links 
        - var slug = navLink.link
          li
            a(href=slug)= navLink.name